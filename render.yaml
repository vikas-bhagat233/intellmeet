services:
  - type: web
    name: intellmeet
    env: node
    plan: free
    buildCommand: |
      # build client, then install server deps
      cd client && npm ci && npm run build
      cd ../server && npm ci
    startCommand: "npm --prefix server start"
