// Email service placeholder - can be extended with nodemailer
export const sendEmail = async (to, subject, body) => {
  console.log(`📧 Sending email to ${to}: ${subject}`)
  // Implementation with nodemailer or SendGrid
  return true
}